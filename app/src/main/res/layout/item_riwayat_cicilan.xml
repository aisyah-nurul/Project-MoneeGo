<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingTop="7dp"
    android:paddingBottom="7dp">

    <!-- FIX BUG 1: icon sekarang dinamis sesuai jenis dompet,
         bukan icon centang generik untuk semua dompet -->
    <FrameLayout
        android:layout_width="26dp"
        android:layout_height="26dp"
        android:background="@drawable/bg_circle_green"
        android:layout_marginEnd="8dp">
        <ImageView
            android:id="@+id/ivRiwayatIcon"
            android:layout_width="14dp"
            android:layout_height="14dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_wallet_bank"
            android:contentDescription="Sumber dana" />
    </FrameLayout>

    <!-- Cicilan ke-N + tanggal -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tvRiwayatTanggal"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Cicilan ke-1 • 1 Apr 2025"
            android:textColor="@color/teks_utama"
            android:textSize="12sp" />

        <TextView
            android:id="@+id/tvRiwayatCatatan"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="-"
            android:textColor="@color/teks_muted"
            android:textSize="10sp" />
    </LinearLayout>

    <!-- Nominal -->
    <TextView
        android:id="@+id/tvRiwayatNominal"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="+Rp0"
        android:textColor="@color/income_green"
        android:textSize="12sp"
        android:textStyle="bold"
        android:layout_marginEnd="8dp"/>

    <!-- Tombol hapus cicilan pakai ic_delete -->
    <ImageButton
        android:id="@+id/btnHapusCicilan"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:src="@drawable/ic_close"
        android:tint="@color/text_primary"
        android:contentDescription="Hapus cicilan"
        android:padding="2dp"/>

</LinearLayout>